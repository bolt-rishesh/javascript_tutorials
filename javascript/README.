#Javascript

1. Timer 
	a. setTimeout() allows you to execute statements ONCE after an interval
	b. clearTimeout() method clears a timer set with the setTimeout() method
	c. setInterval() allows you to execute statements REPEATEDLY after an interval.
	d. clearInterval() method clears a timer set with the setInterval() method.

2. Image fadein and fadeout: use opacity.

3. $("div > p").fadeToggle(1000);
	in this only paragraphs under divisions get fadded.
	->li:even ->.myclass.first also any tags.

// Effects in jquery
4. fadein, fadeout and fadetoggle
   hide, show and toggle
   slidehide, slideshow and slideToggle


// DOM Manipulation:
5. CallBack and Chaining Effect
6. text(), html(), attr() and css();
7. append(), after(), prepend(), before(), remove() and empty().
8. addClass(), removeClass() and toggleClass().


